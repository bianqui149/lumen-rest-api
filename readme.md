## Documentation

```
Clone the lumen framework composer create-project --prefer-dist laravel/lumen blog 

Run from the command line `docker-compose up --build -d`

Update the .env variables to run the migrations
```